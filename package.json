{
  "name": "hello-agent",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "pnpm --filter api start",
    "dev": "pnpm --filter api dev"
  }
}
